/** @type {import('next').NextConfig} */
const nextConfig = {
    output: "export",
    // basePath: "/my-portfolio-site",
    images: {
        unoptimized: true,
    }
};

export default nextConfig;
